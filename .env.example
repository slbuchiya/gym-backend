# Copy this file to `.env` and fill in real credentials (do NOT commit .env)
PORT=5000
# Replace <username>, <password>, <cluster> and <database> with your Atlas values. Remove the angle brackets.
# If your password contains special characters (e.g. @, :, /, ?), URL-encode it (use encodeURIComponent).
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
JWT_SECRET=your_jwt_secret_here
